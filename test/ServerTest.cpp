#define CATCH_CONFIG_MAIN // provides main(); this line is required in only one .cpp file
#include "catch.hpp"

#include <iostream>
#include <sstream>

using namespace std;

TEST_CASE( "UDP SERVER ", "1" ) {
    REQUIRE( true == true );
}